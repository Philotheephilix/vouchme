{
  "name": "vouchme-worldchain-subgraph",
  "version": "0.1.0",
  "private": true,
  "description": "VouchMe — vouchme-worldchain Subgraph. VouchMeRegistry, ReportRegistry, PlatformRegistry, PresenceDrip on World Chain (docs/05-graph-data-layer.md).",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:studio": "graph deploy --studio vouchme-worldchain",
    "deploy:local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 vouchme-worldchain",
    "create:local": "graph create --node http://localhost:8020/ vouchme-worldchain",
    "remove:local": "graph remove --node http://localhost:8020/ vouchme-worldchain",
    "test": "graph test"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.97.1",
    "@graphprotocol/graph-ts": "^0.38.0"
  }
}
