# Symlink the local `file:../vouchme-sdk` dependency instead of copying it, so editing the SDK is
# immediately visible here. Without this, npm >=9 copies the package into node_modules and every
# SDK change needs a reinstall to take effect — which reads as a mystifying type error.
install-links=false
