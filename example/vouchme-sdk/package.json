{
  "name": "@vouchme/minikit-sdk",
  "version": "0.1.0",
  "description": "Read VouchMe standing from a third-party World App mini app. HTTP only — no engine, no chain, no keys.",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc",
    "prepare": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
